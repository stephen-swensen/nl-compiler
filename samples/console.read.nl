open console in
write("enter input: ");
x = int32.minvalue in
while !(x == -1 || x == 10) {
  x <- read();
  writeline("you entered: " + x)
};

x = int32.minvalue in
write("enter input (again!): ");
while !(x == -1 || x == 10) {
  x <- read();
  writeline("you entered: " + x)
};

writeline("that's it")